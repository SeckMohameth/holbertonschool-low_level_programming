#include "holberton.h"
/**
* _puts_recursion - print string
* @s: pointer to string
* Returns: always 0
**/
void _puts_recursion(char *s)
{
	int i;
	char *p = s;

	for (i = 0; p[i] != 0; i++)
	{
		_putchar(p[i]);
		 
	}

	_putchar('\n');
}
