#include "binary_trees.h"
/*
 * binary_tree_balance - measures factor of balnce tree
 * @tree: is a pointer to the root node of the tree to measure the balance
 * Return: 
 */

int binary_tree_balance(const binary_tree_t *tree)
{
	

	if (tree == NULL)
		return (0);

}
